# Prism — 光と色のパズルゲーム

## プロジェクト概要
iOS向けの2Dパズルゲーム。光線を鏡・プリズム・フィルタで導き、ゴールの結晶に
「指定された色の光」を届けるとステージクリア。
「Electric Box」系の連鎖変換パズルを、電気ではなく **光と色(RGB加法混色)** に
置き換えて独自化したもの。ジャンル(連鎖変換パズル)は流用しつつ、テーマ・部品・
名称・コア変数(色)を独自にすることでオリジナル作品にする方針。

## 技術スタック
- 言語: Swift(最新安定版)
- UI: SwiftUI(メニュー・ステージ選択)+ SpriteKit(盤面・ビーム描画。`SpriteView`で埋め込む)
- 対象: iOS 17+ / iPhone 縦持ちを主対象
- アーキテクチャ: MVVM + `@Observable`

## 設計の基本方針
- 物理シミュレーションは行わない。光線は **レイキャスト(直線の線分)** で表現し、
  盤面が変化したときだけ再計算する(パフォーマンスと解の明確さを両立)。
- 色は `SIMD3<Float>`(RGB, 各成分 0...1)で表現。合流は **加法混色(加算してクランプ)**。
  ゴールは受け取った色と目標色の誤差が閾値内なら作動。
- ステージは **Codable な JSON** で定義し、量産できるようにする。

## コーディング規約
- ドキュメント・コメントは日本語、型名/変数名は英語。
- ステージデータは `Resources/Levels/*.json` に配置。
- 1機能ずつ動く状態を保ちながら実装する(下記マイルストーン順)。

## 詳細仕様
- ゲーム全体の設計・部品仕様・アルゴリズム・実装手順は `spec_prism.md` を参照すること。

## このプロジェクトの進め方(Claude Codeへの指示)
- まず `spec_prism.md` を読み、マイルストーン1から順に実装する。
- 各マイルストーン完了時に一度動作確認できる形にしてから次へ進む。
